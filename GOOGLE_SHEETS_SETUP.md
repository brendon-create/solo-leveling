# 📊 Google Sheets 設置指南

本指南將教你如何設置 Google Sheets 來儲存 Solo Leveling 的數據。

## 🎯 為什麼使用 Google Sheets？

- ✅ **完全掌控數據** - 數據儲存在你自己的 Google 帳號中
- ✅ **無需後端伺服器** - 降低成本和複雜度
- ✅ **方便分析** - 可以使用 Google Sheets 強大的分析功能
- ✅ **自動備份** - Google 自動處理備份
- ✅ **多人共享** - 未來可以輕鬆分享給教練或夥伴

## 📋 快速設置（3 步驟）

### 方式 A：最簡單方式（推薦初學者）

#### 步驟 1：創建 Google Sheet

1. 開啟 [Google Sheets](https://sheets.google.com)
2. 點擊 **「+」空白** 創建新試算表
3. 將試算表命名為「Solo Leveling - 個人追蹤」

#### 步驟 2：設置權限

1. 點擊右上角的 **「共用」** 按鈕
2. 點擊 **「變更」**（在「限制存取」旁邊）
3. 選擇 **「知道連結的任何人」**
4. 選擇 **「編輯者」** 權限
5. 點擊 **「完成」**

⚠️ **重要**：必須設置為「編輯者」權限，應用才能寫入數據。

#### 步驟 3：複製 URL 到應用

1. 複製瀏覽器網址列中的完整 URL（類似：`https://docs.google.com/spreadsheets/d/xxxxx/edit`）
2. 開啟 Solo Leveling 應用
3. 在設置頁面貼上 URL
4. 點擊「開始冒險」

✅ **完成！** 應用會自動在你的 Sheet 中創建所需的欄位。

---

## 🔧 方式 B：進階設置（Google Apps Script）

如果你想要更進階的功能（如自動計算、圖表、提醒等），可以使用 Google Apps Script。

### 步驟 1：創建並設置 Sheet（同上）

按照方式 A 的步驟 1-2 完成基礎設置。

### 步驟 2：創建 Apps Script

1. 在你的 Google Sheet 中，點擊 **擴充功能** → **Apps Script**
2. 刪除預設的代碼
3. 複製並貼上以下代碼：

```javascript
function doPost(e) {
  try {
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
    const data = JSON.parse(e.postData.contents);
    
    // 檢查是否已有表頭
    if (sheet.getLastRow() === 0) {
      initializeSheet(sheet);
    }
    
    // 添加數據
    const row = [
      new Date(),
      data.str?.jogging || false,
      data.str?.weightTraining || false,
      data.str?.hiit || false,
      data.hp?.water || 0,
      data.hp?.wakeTime || '',
      data.hp?.sleepTime || '',
      data.hp?.meals || '',
      data.int?.reading || false,
      data.int?.italian || false,
      data.int?.course || false,
      data.mp?.scripture || false,
      data.mp?.prayer || false,
      data.mp?.journal || false,
      data.crt?.piano || false,
      data.crt?.drawing || false,
      data.gold?.income || 0,
      data.gold?.action1 || false,
      data.gold?.action2 || false,
      data.gold?.action3 || false,
      data.rsn?.gratitude || '',
      data.alcohol?.reason || '',
      data.alcohol?.feeling || ''
    ];
    
    sheet.appendRow(row);
    
    return ContentService.createTextOutput(JSON.stringify({
      success: true,
      message: '數據已儲存'
    })).setMimeType(ContentService.MimeType.JSON);
    
  } catch (error) {
    return ContentService.createTextOutput(JSON.stringify({
      success: false,
      error: error.toString()
    })).setMimeType(ContentService.MimeType.JSON);
  }
}

function initializeSheet(sheet) {
  const headers = [
    '日期',
    'STR_慢跑', 'STR_重訓', 'STR_HIIT',
    'HP_飲水(cc)', 'HP_起床時間', 'HP_就寢時間', 'HP_飲食',
    'INT_閱讀', 'INT_義大利文', 'INT_課程',
    'MP_讀經', 'MP_禱告', 'MP_日記',
    'CRT_練琴', 'CRT_畫畫',
    'GOLD_收入', 'GOLD_行動1', 'GOLD_行動2', 'GOLD_行動3',
    'RSN_感恩筆記',
    '酒精_理由', '酒精_感受'
  ];
  
  sheet.appendRow(headers);
  
  // 格式化表頭
  const headerRange = sheet.getRange(1, 1, 1, headers.length);
  headerRange.setFontWeight('bold');
  headerRange.setBackground('#9333ea');
  headerRange.setFontColor('#ffffff');
}

function doGet(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = sheet.getDataRange().getValues();
  
  return ContentService.createTextOutput(JSON.stringify({
    success: true,
    data: data
  })).setMimeType(ContentService.MimeType.JSON);
}
```

### 步驟 3：部署 Web App

1. 點擊 **部署** → **新增部署作業**
2. 選擇類型：**網頁應用程式**
3. 設定：
   - 說明：「Solo Leveling API」
   - 執行身分：**我**
   - 具有存取權的使用者：**所有人**
4. 點擊 **部署**
5. 授權應用程式（可能需要點擊「進階」→「前往...」）
6. **複製 Web App URL**（類似：`https://script.google.com/macros/s/xxxxx/exec`）

### 步驟 4：在應用中使用

目前應用使用基礎模式（直接編輯 Sheet），如果你設置了 Apps Script，可以修改 `src/services/googleSheets.js` 來使用 Web App URL。

---

## 📊 數據結構說明

應用會在你的 Sheet 中創建以下欄位：

| 欄位 | 類型 | 說明 |
|------|------|------|
| 日期 | 日期 | 記錄日期 |
| STR_慢跑/重訓/HIIT | 布林值 | 運動完成狀態 |
| HP_飲水 | 數字 | 飲水量(cc) |
| HP_起床/就寢時間 | 文字 | ok/great/best |
| HP_飲食 | 文字 | 飲食等級 |
| INT_閱讀/義大利文/課程 | 布林值 | 學習完成狀態 |
| MP_讀經/禱告/日記 | 布林值 | 靈性活動完成狀態 |
| CRT_練琴/畫畫 | 布林值 | 創作活動完成狀態 |
| GOLD_收入 | 數字 | 當日收入 |
| GOLD_行動1/2/3 | 布林值 | 事業行動完成狀態 |
| RSN_感恩筆記 | 文字 | 感恩內容 |
| 酒精_理由/感受 | 文字 | 飲酒記錄 |

## 📈 進階分析

設置完成後，你可以在 Google Sheets 中：

### 1. 創建圖表
- 選擇數據範圍
- 點擊「插入」→「圖表」
- 選擇折線圖追蹤長期趨勢

### 2. 使用公式計算
```
=COUNTIF(B2:B, TRUE)  // 計算慢跑總天數
=AVERAGE(E2:E)        // 平均飲水量
=SUM(Q2:Q)            // 總收入
```

### 3. 條件格式化
- 選擇欄位
- 點擊「格式」→「條件格式設定」
- 設置規則（如：飲水 < 2400 顯示紅色）

### 4. 創建儀表板
- 新增工作表命名為「儀表板」
- 使用 `QUERY`, `FILTER`, `ARRAYFORMULA` 等函數
- 插入圖表和摘要統計

## 🔒 隱私與安全

### 誰可以看到我的數據？

- 設置為「知道連結的任何人」時，只有擁有 URL 的人可以存取
- 不要公開分享你的 Sheet URL
- 隨時可以在「共用」設定中撤銷權限

### 如何提升安全性？

1. **使用 Apps Script**（方式 B）- 應用只能透過 API 寫入，無法直接編輯
2. **定期更改 URL** - 可以創建新 Sheet 並更新應用設定
3. **限制編輯** - 完成設置後，改為「檢視者」權限（但應用無法寫入）

### 數據備份

Google 自動備份，但你也可以：
- 定期「下載」為 Excel 或 CSV
- 使用 Google Takeout 匯出所有數據

## ❓ 疑難排解

### 問題 1：應用無法連接 Sheet

**可能原因**：
- URL 格式錯誤
- 權限設置為「僅限檢視」
- Sheet 已被刪除

**解決方法**：
1. 確認 URL 是完整的（包含 `/edit` 部分）
2. 重新檢查權限設置
3. 嘗試創建新的 Sheet

### 問題 2：數據沒有同步

**可能原因**：
- 網路連線問題
- 瀏覽器阻擋跨域請求

**解決方法**：
1. 檢查網路連線
2. 打開瀏覽器開發者工具（F12）查看錯誤訊息
3. 確認應用有正確的 Sheet URL

### 問題 3：Apps Script 部署失敗

**可能原因**：
- 未授權應用程式
- Google 帳號權限限制

**解決方法**：
1. 重新授權（點擊「進階」→「前往...」）
2. 確認 Google 帳號沒有組織政策限制
3. 嘗試使用個人 Google 帳號（非企業帳號）

## 💡 最佳實踐

1. **定期檢查數據** - 每週查看 Sheet 確保數據正確同步
2. **不要手動編輯** - 除非必要，避免直接在 Sheet 中修改數據
3. **創建副本** - 每月創建一個備份副本
4. **使用單獨的 Sheet** - 每個應用使用獨立的 Sheet，避免混淆
5. **命名清楚** - 給 Sheet 一個描述性的名稱

---

## 📞 需要幫助？

- 查看 [Google Sheets 說明中心](https://support.google.com/docs/answer/6000292)
- 查看 [Apps Script 文檔](https://developers.google.com/apps-script)
- 在 GitHub 上提交 [Issue](https://github.com/brendon-create/solo-leveling/issues)

---

**設置完成後，開始你的升級之旅！** ⚔️
